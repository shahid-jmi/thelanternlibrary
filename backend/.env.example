MONGO_URI=mongodb://localhost:27017/bookstore
PORT=5000
JWT_SECRET=your_secret_key_here
CORS_ORIGIN=http://localhost:5173

# Admin accounts are no longer configured via env vars.
# Create the first super admin with: node createSuperAdmin.js
# Create subsequent admins via POST /api/admin/admins (requires an existing super admin).
